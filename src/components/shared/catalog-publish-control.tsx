"use client";
import { useState } from "react";
export function CatalogPublishControl({ productId, initial, name }: { productId: string; initial: boolean; name: string }) { const [checked, setChecked] = useState(initial); const [saving, setSaving] = useState(false); async function change(next: boolean) { const previous = checked; setChecked(next); setSaving(true); const r = await fetch("/api/website/products", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ productId, isPublishedOnline: next }) }); if (!r.ok) setChecked(previous); setSaving(false); } return <label className="flex cursor-pointer items-center justify-between gap-3 rounded-xl border border-slate-100 px-3 py-2 text-sm"><span className="truncate">{name}</span><input aria-label={`Publish ${name}`} type="checkbox" checked={checked} disabled={saving} onChange={(e) => change(e.target.checked)} className="size-4" /></label>; }
